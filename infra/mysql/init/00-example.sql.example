-- Exemple de script d'initialisation MySQL
-- Renommez ce fichier en .sql pour qu'il soit exécuté au démarrage
-- Les scripts sont exécutés dans l'ordre alphabétique

-- Exemple: Créer une table
-- CREATE TABLE IF NOT EXISTS users (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     username VARCHAR(255) NOT NULL UNIQUE,
--     email VARCHAR(255) NOT NULL UNIQUE,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- Exemple: Insérer des données de test
-- INSERT INTO users (username, email) VALUES
--     ('admin', 'admin@poo.localhost'),
--     ('user', 'user@poo.localhost');
